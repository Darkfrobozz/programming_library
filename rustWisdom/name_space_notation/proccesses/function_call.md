Function calls are when we actually using the function, importantly, function calls are treated as [[expression]]s.

For us to actually do something with a function call we need a matching [[function_signature]].
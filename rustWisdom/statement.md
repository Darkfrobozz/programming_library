This performs some action and does not return a value.

The fact that some code does not return values is incredibly important because it means it cannot be combined as input to other code.

These end with ';'
